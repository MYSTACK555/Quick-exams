<!DOCTYPE html>
<html lang="{{languages.language}}">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src = "/js/changeLang.js"></script>
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
    <script id="mcjs">!function(c,h,i,m,p){m=c.createElement(h),p=c.getElementsByTagName(h)[0],m.async=1,m.src=i,p.parentNode.insertBefore(m,p)}(document,"script","https://chimpstatic.com/mcjs-connected/js/users/3b137f2f98f7f0263123e54fe/207d046f8016a35853778ef24.js");</script>
    <link rel="stylesheet" href="/css/style.css"/>
    <link rel="icon" type="image/x-icon" href="/asset/exam.svg" />
</head>

<body>
    <header>
        <nav>

            <div class="logo">
                <a href="/">
                    <img src="/asset/exam.svg" alt="quick-exams logo" width="50" />
                </a>
            </div>
            <ul>
                <li><a href="/{{languages.language}}/">{{languages.traductionFile.accueil}}</a></li>
                <li><a href="/{{languages.language}}/about.html">{{languages.traductionFile.contact}}</a></li>
                <li><a href="/{{languages.language}}/services.html">{{languages.traductionFile.service}}</a></li>
            </ul>
            <label for="lang">{{languages.traductionFile.selectLanguage}}</label>

            <select name="lang" id="lang">
            {% if languages.language == "FR"%}
                {% for lang in supportedLanguages %}
                <option value="{{lang}}">{{lang}}</option>
                {% endfor %}
            {% else %}
                {% for lang in supportedLanguages|reverse %}
                <option value="{{lang}}">{{lang}}</option>
                {% endfor %}
            {% endif %}
            </select>
            <div data-netlify-identity-menu></div>
            <div data-netlify-identity-button>Login with Netlify Identity</div>
        </nav>
    </header>

    <div class="container">
        {{content|safe}}
    </div>

    <footer>
        <div>Copyright © 2021 Quick Exams - {{ languages.traductionFile.allrightReserved }}.</div>
    </footer>
</body>

</html>